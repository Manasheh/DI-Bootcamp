import React from "react"


const Hello = ({name}) => {
    // console.log(props);
    return (
        <React.Fragment>
        <h1>Hello to {name}</h1>
        
        </React.Fragment>
    )
}

export default Hello